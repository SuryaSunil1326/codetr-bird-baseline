expected layout, COCO format:

```
data/
  annotations/
    instances_train.json
    instances_val.json
  images/
    train/
    val/
```

annotation files use standard COCO instance segmentation format, single category.
